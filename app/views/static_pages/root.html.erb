<% if signed_in? %>
    <script>
        console.log("<%= current_user.username %>")
        window.currentUser = {
            "id": <%= current_user.id %>,
            "username": "<%= current_user.username %>",
            "email": "<%= current_user.email %>"
        };
    </script>
<% end %>

<main id="root"></main>
